# Host-side tooling: the unit tests, the linter, and the helper scripts
# (checkQueue.py, testIt.py). None of this is installed in the image; the
# tests stub the GPU stack and run on any machine.
#
#   uv pip install -r requirements-dev.txt
#   python -m pytest tests/ -q
#   ruff check .
requests>=2.32
pytest>=8.0
pytest-cov>=5.0
ruff>=0.5
# app.py imports Flask and transcribe.py imports numpy at module import time;
# the tests stub torch, faster-whisper and pydub but need these two for real.
Flask>=3.1
numpy>=1.26
